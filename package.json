{
  "name": "museums-sunday-bot",
  "version": "1.0.0",
  "description": "Telegram bot for booking free museum tickets every first Sunday of the month",
  "main": "index.js",
  "author": "Ben Adrian Sarmiento <me@bensarmiento.com>",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "build:worker": "lerna run build --scope=worker",
    "release": "lerna version minor -y",
    "publish": "lerna publish -y from-git",
    "lint": "lerna run lint",
    "format": "prettier --write 'src/**/*.{js,jsx,ts,tsx,json,md}'"
  },
  "devDependencies": {
    "lerna": "^6.6.1",
    "prettier": "^2.8.8",
    "prettier-plugin-organize-imports": "^3.2.2",
    "prettier-plugin-sort-json": "^1.0.0",
    "wrangler": "^2.17.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
